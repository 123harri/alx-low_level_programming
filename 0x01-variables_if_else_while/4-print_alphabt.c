#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase and uppercase
 * Return: Always 0
 */
int main(void)
{
char m;
for (m = 'a'; m <= 'z'; m++)
{
if (m != 'e' && m != 'q')
}
putchar(h);
putchar('\n');
return (0);
}
